apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms-service
  namespace: omplatform
  labels:
    app: wms-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wms-service
  template:
    metadata:
      labels:
        app: wms-service
    spec:
      terminationGracePeriodSeconds: 45
      containers:
        - name: wms-service
          image: omplatform/wms-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
              name: http
          envFrom:
            - configMapRef:
                name: wms-service-config
            - configMapRef:
                name: infra-endpoints
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 30
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1"