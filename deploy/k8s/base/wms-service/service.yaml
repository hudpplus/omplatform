apiVersion: v1
kind: Service
metadata:
  name: wms-service
  namespace: omplatform
  labels:
    app: wms-service
spec:
  selector:
    app: wms-service
  ports:
    - port: 8082
      targetPort: 8082
      name: http
  type: ClusterIP