apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: omplatform
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  type: ClusterIP