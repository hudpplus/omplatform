apiVersion: v1
kind: Service
metadata:
  name: seckill-service
  namespace: omplatform
  labels:
    app: seckill-service
spec:
  selector:
    app: seckill-service
  ports:
    - port: 8082
      targetPort: 8082
      name: http
  type: ClusterIP