spring:
  application:
    name: wms-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
      config:
        server-addr: localhost:8848
        namespace: public
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
            refresh: true
          - data-id: wms-service.yaml
            refresh: true
